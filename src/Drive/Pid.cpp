#include "Pid.h"

// PID logic is inlined in Pid.h. This file exists to
// satisfy naming preference (pid.cpp) and some build setups.
#include "Pid.h"

// All methods are inline in the header for simplicity and performance.
// This translation unit exists to satisfy build systems expecting Pid.cpp.
